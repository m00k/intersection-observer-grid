import { FunctionalComponent, h } from 'preact';
import * as VM from '../api/model';

const App: FunctionalComponent = () => {
    return (
        <div id="preact_root">
            TODO
        </div>
    );
};

export default App;
